package sysdefine

const (
	// ServerTypeCache is the server type of the cache server
	ServerTypeCache = iota + 1
	// ServerTypeLogin is the server type of the login server
	ServerTypeLogin
	// ServerTypeRegion is the server type of the region server
	ServerTypeRegion
)
